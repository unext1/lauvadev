version: '3.6'
services:
  website:
    build: 
      context: .
    container_name: lauva_website
    restart: unless-stopped
    environment:
      RESEND_API_KEY: ${RESEND_API_KEY}
    networks:
      - proxy
    ports:
      - 3000:3000

networks:
  proxy:
    external: true